REG ${BASE} IPC${INDEX} CLR SET INV
${if ${P1} != UNUSED ? BIT 28-26       ${P1}IP}
${if ${P1} != UNUSED ? BIT 25-24       ${P1}IS}
${if ${P2} != UNUSED ? BIT 20-18       ${P2}IP}
${if ${P2} != UNUSED ? BIT 17-16       ${P2}IS}
${if ${P3} != UNUSED ? BIT 12-10       ${P3}IP}
${if ${P3} != UNUSED ? BIT 9-8         ${P3}IS}
${if ${P4} != UNUSED ? BIT 4-2         ${P4}IP}
${if ${P4} != UNUSED ? BIT 1-0         ${P4}IS}
