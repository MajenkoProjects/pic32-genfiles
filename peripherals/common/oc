
PERIPHERAL +{${BASE},00} OCMP${INDEX}
REG +{${BASE},00} OC${INDEX}CON CLR SET INV
BIT 15      ON
BIT 13      SIDL
BIT 5       OC32
BIT 4       OCFLT
BIT 3       OCTSEL
BIT 2-0     OCM

REG +{${BASE},10} OC${INDEX}R WHOLE CLR SET INV

REG +{${BASE},20} OC${INDEX}RS WHOLE CLR SET INV

