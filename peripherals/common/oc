
PERIPHERAL ${rpn ${BASE} 0x00 +} OCMP${INDEX}
REG ${rpn ${BASE} 0x00 +} OC${INDEX}CON CLR SET INV
BIT 15      ON
BIT 13      SIDL
BIT 5       OC32
BIT 4       OCFLT
BIT 3       OCTSEL
BIT 2-0     OCM

REG ${rpn ${BASE} 0x10 +} OC${INDEX}R WHOLE CLR SET INV

REG ${rpn ${BASE} 0x20 +} OC${INDEX}RS WHOLE CLR SET INV

